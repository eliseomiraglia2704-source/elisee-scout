# 09-auth-spid-logo

## Convenzione
Se l'utente dice **"aggiorna"** → le nuove immagini sono già al posto delle vecchie (stesso nome file).
L'assistente fa solo scattare il refresh sul sito (cache-bust), senza generare nuove immagini.

## Cartelle
- corrente/   = versione attiva
- originali/  = backup iniziale
- modifiche/  = bozze

## File linkato dal sito
Nella root di questa cartella (non dentro le sottocartelle).
